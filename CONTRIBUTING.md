# Contributing

Thanks for helping improve Universal Document Workbench.

## Local Setup

```sh
make install-hooks
make dev
make test
make smoke
```

Use Conventional Commits for every commit:

```text
feat: add document upload flow
fix: handle empty OCR output
docs: explain Pages deployment
```

Do not commit secrets, private keys, `.env` files, generated logs, or local build caches.

